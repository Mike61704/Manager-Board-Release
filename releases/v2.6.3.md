# Manager Board v2.6.3 — Overview History Totals

This maintenance update keeps current-year and imported history totals together in Export Overview PNG.

## Fixes

- Normal Workfront synchronization retains imported prior-year project totals.
- Importing a history year preserves current task and project totals in the Overview.
- Repeated history imports replace that year's totals without adding duplicate projects.
- Reopened projects and changed completion years are reconciled using recorded project identifiers.
- Existing trusted import evidence is used where available without changing saved project records.

The Overview layout is unchanged. Schema remains 48 with no new migration. Workspace/Vault separation and privacy boundaries remain intact. Optional Local AI models remain separate and are not bundled.

## Validation

All 2,646 regression tests, 109 package checks, and 20 real-model quality cases passed. The focused reporting and Workfront regression passed 113 tests, and the product owner accepted desktop source smoke testing. The supported release pipeline verifies runtime and trusted application, native runtime, embedded installer, uninstaller, and final installer signing before publishing the installer.

A fresh public download matched the published size and SHA-256 digest and retained a valid trusted signature. Normal Windows installation succeeded; all 33 installed signatures and the packaged runtime check passed. Installed Test Workspace smoke exported an Overview containing current task totals and both current-year and prior-year project totals. The installed export was byte-identical to the accepted source export. Live Workfront authentication and synchronization were not repeated during installed smoke.

## Installer

Download `ManagerBoard-Setup-2.6.3.exe` from this release's Assets section.

SHA-256: `6A9586499602BEBA41A145D2EF32EF6F89A48D9CE627992C3DEC7EADF9B23F19`
